{
  "name": "static.joreteg.com",
  "version": "1.2.0",
  "author": "Henrik Joreteg <henrik@joreteg.com>",
  "dependencies": {
    "surge": "^0.11.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/henrikjoreteg/static.joreteg.com"
  },
  "scripts": {
    "commit": "git add --all && git commit -am \"$(date)\" && npm version minor && git push origin master --tags",
    "deploy": "surge -p www -d static.joreteg.com",
    "ship": "npm run commit && npm run deploy"
  }
}
